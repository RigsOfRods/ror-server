import os, glob

Import("env")
localenv = env.Copy()
localenv.Append(CPPPATH = ["#/SocketW/src"])
localenv.Append(LIBS = ["libmysocketw", "pthread"])
localenv.Append(LIBPATH=["#/SocketW/src"])

localenv.Program("rorserver", glob.glob('*.cpp'))

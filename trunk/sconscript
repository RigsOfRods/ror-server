import os, glob

Import("env")
localenv = env.Copy()
localenv.Append(CPPPATH = ["#/mysocketw"])
localenv.Append(LIBS = ["libmysocketw", "pthread"])
localenv.Append(LIBPATH=["#/mysocketw"])

localenv.Program("rorserver", glob.glob('*.cpp'))
